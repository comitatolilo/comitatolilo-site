<!DOCTYPE html>
<html lang="it" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Titolo pagina – Comitato Liceo Locarno</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root{
      --bg-grad: radial-gradient(1200px 800px at 10% 10%, #101421 0%, #0b1220 40%, #080b12 100%);
      --surface:#111318; --text:#fff; --text-2:#bdbdbd; --link:#7bd4ff;
      --shadow:0 8px 30px rgba(0,0,0,.25); --radius:12px;
      --card-bg:#000; --card-fg:#fff;
      --blue:#4a90e2; --red:#d81313; --brown:#8B4513; --rose:#ba454f;
    }
    [data-theme="light"]{
      --bg-grad: radial-gradient(1000px 700px at 85% 10%, #ffffff 0%, #f3f7fc 45%, #eaeff6 100%);
      --surface:#ffffff; --text:#121212; --text-2:#555; --link:#0066cc;
      --shadow:0 8px 30px rgba(0,0,0,.08);
      --card-bg:#fff; --card-fg:#111;
    }
    @property --angle { syntax: "<angle>"; inherits: false; initial-value: 0deg; }
    *{box-sizing:border-box}
    html,body{margin:0;background:var(--bg-grad);color:var(--text);font-family:Poppins,system-ui,sans-serif}
    a{color:var(--link);text-decoration:none}

    /* header + sidebar */
    .header{position:fixed;inset:0 0 auto 0;height:64px;display:flex;align-items:center;justify-content:space-between;background:var(--surface);box-shadow:var(--shadow);padding:0 .75rem;z-index:1000}
    .iconbtn{all:unset;cursor:pointer;padding:.55rem .7rem;border-radius:.8rem}
    .iconbtn:hover{background:rgba(255,255,255,.06)} [data-theme="light"] .iconbtn:hover{background:rgba(0,0,0,.06)}
    .brand a{color:var(--text);font-weight:600}
    .sidebar{position:fixed;top:0;left:0;height:100dvh;width:min(290px,85vw);background:var(--surface);box-shadow:0 0 40px rgba(0,0,0,.4);transform:translateX(-100%);transition:transform .28s;z-index:1100;display:flex;flex-direction:column;padding:14px}
    .sidebar.open{transform:translateX(0)}
    .side-links{display:flex;flex-direction:column;gap:.5rem;padding:10px 4px}
    .side-links a{color:var(--text);padding:.7rem .9rem;border-radius:.8rem;border:1px solid #ffffff10}
    .side-links a:hover{background:#ffffff10;border-color:#ffffff22} [data-theme="light"] .side-links a:hover{background:#0001;border-color:#0002}

    /* layout */
    .main{padding-top:80px}
    .wrap{max-width:980px;margin:0 auto;padding:18px}
    .hero{display:grid;place-items:center;margin:18px 0 6px}
    .hero-logo{width:180px;height:180px;border-radius:50%;background:url(/img/logo-comitatolilo.png) center/cover no-repeat;box-shadow:0 0 200px rgba(0,0,0,.35)}
    .title{font-size:clamp(1.8rem,4vw,3rem);text-align:center;margin:10px 0 20px}

    /* cards */
    .grid{display:grid;grid-template-columns:repeat(2,1fr);gap:18px}
    .card{position:relative;background:var(--card-bg);color:var(--card-fg);border:2px solid transparent;border-radius:var(--radius);box-shadow:var(--shadow);min-height:160px;display:flex;align-items:center;justify-content:center;text-align:center;padding:18px;transition:transform .2s,background .2s,color .2s;overflow:hidden}
    .card:hover{transform:translateY(-2px)}
    .card h3{margin:.4rem 0 0}

    /* varianti bordo statico */
    .card--talks{border-color:var(--blue)}
    .card--cine{border-color:var(--red)}
    .card--autog{border-color:var(--brown)}
    .card--lilo{border-color:var(--rose)}

    /* biglietti: bordo arcobaleno spesso, fondo bianco/nero a riposo */
    .card--tickets{background:var(--card-bg);border-color:transparent}
    .card--tickets>*{position:relative;z-index:1}
    .card--tickets::before{
      content:"";position:absolute;inset:-10px;border-radius:calc(var(--radius) + 10px);
      background:conic-gradient(from var(--angle),#ff004c,#ff7a00,#ffd400,#21d300,#00d0ff,#7a00ff,#ff00a8,#ff004c);
      padding:10px;
      -webkit-mask:linear-gradient(#000 0 0) content-box,linear-gradient(#000 0 0);
      -webkit-mask-composite:xor;mask-composite:exclude;
      animation:rainbow 6s linear infinite;
    }
    @keyframes rainbow{to{--angle:360deg}}

    /* hover pieni in tinta scura */
    .card--talks:hover{background:#0a2b4f;color:#fff}
    .card--cine:hover{background:#4a0a0a;color:#fff}
    .card--autog:hover{background:#2f1a0e;color:#fff}
    .card--lilo:hover{background:#8b0014;color:#fff}
    .card--tickets:hover{background:#0b3e4a;color:#fff}

    /* sezione “Chi siamo” */
    .section{background:var(--surface);border-radius:var(--radius);box-shadow:var(--shadow);padding:20px;margin:26px 0}
    .section h2{margin:.2rem 0 1rem}

    /* footer con contatti */
    footer{max-width:980px;margin:24px auto;padding:0 18px 18px;color:var(--text-2);display:flex;flex-wrap:wrap;gap:10px 18px;align-items:center;justify-content:space-between}
    .footer-links{display:flex;gap:14px;flex-wrap:wrap}
    .footer-links a{color:var(--text-2)}
    .footer-links a:hover{text-decoration:underline}

    /* responsive */
    @media (max-width:900px){
      .grid{grid-template-columns:1fr}
      .card{min-height:150px}
      .card--lilo{min-height:auto;padding-block:22px}
      .card--lilo img{width:84px;height:84px;object-fit:contain}
    }
  </style>
</head>
<body>
  <aside id="sidebar" class="sidebar" aria-hidden="true">
    <div style="display:flex;align-items:center;justify-content:space-between;padding:6px 4px">
      <div class="brand"><a href="/">Comitato Lilo</a></div>
      <button id="closeMenu" class="iconbtn" aria-label="Chiudi menu">✕</button>
    </div>
    <nav class="side-links">
      <a href="/">Home</a>
      <a href="/tickets">Biglietti / Feste</a>
      <a href="/liltalks/index.html">LiLtalks</a>
      <a href="/cinelilo/index.html">CineLiLo</a>
      <a href="/autogestite/autogestite.html">Autogestite</a>
      <a href="/contacts">Contatti</a>
      <a href="/imprint.html">Imprint</a>
      <a href="/privacy.html">Privacy</a>
    </nav>
  </aside>

  <header class="header">
    <button id="openMenu" class="iconbtn" aria-label="Apri menu">☰</button>
    <div class="brand"><a href="/">Comitato Liceo Locarno</a></div>
    <button id="themeToggle" class="iconbtn" aria-label="Cambia tema">🌓</button>
  </header>

  <main class="main">
    <div class="wrap">
      <div class="hero"><div class="hero-logo" aria-hidden="true"></div></div>
      <h1 class="title">Titolo della pagina</h1>

      <div class="grid">
        <a class="card card--tickets" href="https://tickets.comitatolilo.ch" target="_blank" rel="noopener">
          <div>
            <div style="letter-spacing:.12em;opacity:.9">VENDITA</div>
            <h3 style="margin:.2rem 0 0">Biglietti Feste</h3>
          </div>
        </a>
        <a class="card card--talks" href="/liltalks/index.html">
          <h3>LiLtalks</h3>
        </a>
        <a class="card card--cine" href="/cinelilo/index.html">
          <h3>CineLiLo</h3>
        </a>
        <a class="card card--autog" href="/autogestite/autogestite.html">
          <h3>Autogestite</h3>
        </a>
        <a class="card card--lilo" href="https://liloscambio.ch/default.php" target="_blank" rel="noopener">
          <div>
            <h3>LiLoScambio</h3>
            <p style="margin:.3rem 0 0;opacity:.85">Servizio indipendente degli studenti; policy e responsabilità sul loro sito.</p>
          </div>
        </a>
      </div>

      <section class="section">
        <h2>Chi siamo</h2>
        <p>Siamo il Comitato Studenti del Liceo di Locarno. Organizziamo eventi, attività culturali e sociali per la comunità scolastica. Per contattarci e per richieste ufficiali usa sempre la pagina dedicata: <a href="/contacts">comitatolilo.ch/contacts</a>.</p>
        <p>LiLoScambio è un progetto esterno e indipendente, creato da studenti del Liceo. Non è sviluppato né gestito da noi; lo segnaliamo per promuoverne l’utilità, ma privacy, condizioni e responsabilità sono esclusivamente indicate sul loro sito.</p>
      </section>
    </div>
  </main>

  <footer>
    <div class="footer-links">
      <a href="/contacts">Contatti</a>
      <a href="/privacy.html">Privacy</a>
      <a href="/imprint.html">Imprint</a>
    </div>
    <div>© Comitato Studenti Liceo Locarno</div>
  </footer>

  <script>
    (function initTheme(){
      const root=document.documentElement,stored=localStorage.getItem('theme');
      if(stored==='light'||stored==='dark') root.setAttribute('data-theme',stored);
      else root.setAttribute('data-theme',window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');
      const mq=window.matchMedia('(prefers-color-scheme: dark)');
      mq.addEventListener?.('change',e=>{ if(!localStorage.getItem('theme')) document.documentElement.setAttribute('data-theme',e.matches?'dark':'light'); });
    })();
    const sb=document.getElementById('sidebar');
    document.getElementById('openMenu').addEventListener('click',()=>{sb.classList.add('open');sb.setAttribute('aria-hidden','false')});
    document.getElementById('closeMenu').addEventListener('click',()=>{sb.classList.remove('open');sb.setAttribute('aria-hidden','true')});
    document.addEventListener('keydown',e=>{if(e.key==='Escape') sb.classList.remove('open')});
    document.getElementById('themeToggle').addEventListener('click',()=>{const r=document.documentElement,n=r.getAttribute('data-theme')==='dark'?'light':'dark';r.setAttribute('data-theme',n);localStorage.setItem('theme',n)});
  </script>
</body>
</html>
